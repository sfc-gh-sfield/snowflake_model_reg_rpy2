% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{sfr_predict_local}
\alias{sfr_predict_local}
\title{Test an R model locally}
\usage{
sfr_predict_local(
  model,
  new_data,
  predict_fn = "predict",
  predict_pkgs = character(0),
  predict_body = NULL,
  input_cols = NULL,
  output_cols = NULL
)
}
\arguments{
\item{model}{An R model object.}

\item{new_data}{A data.frame with input data.}

\item{predict_fn}{Character. R function name for inference. Default:
\code{"predict"}.}

\item{predict_pkgs}{Character vector. R packages to load before prediction.}

\item{predict_body}{Character. Optional custom R code. Use template
variables \code{{{MODEL}}}, \code{{{INPUT}}}, \code{{{UID}}}, \code{{{N}}} (same as used
in the Python bridge).}

\item{input_cols}{Named list. Input column schema (for validation only).}

\item{output_cols}{Named list. Output column schema (for validation only).}
}
\value{
A data.frame with predictions.
}
\description{
Calls the R predict function directly on the model, exactly as the
registered model would behave inside Snowflake. Use this to verify
predictions before registering.
}
\details{
\strong{Note:} This runs entirely in R (no Python bridge). The Python bridge
with rpy2 is only used when the model executes inside Snowflake (SPCS).
Calling reticulate -> Python -> rpy2 -> R from within R would cause a
dual-runtime segfault.
}
\examples{
\dontrun{
model <- lm(mpg ~ wt, data = mtcars)
preds <- sfr_predict_local(model, data.frame(wt = c(2.5, 3.0, 3.5)))
}

}
\seealso{
\code{\link[=sfr_log_model]{sfr_log_model()}}
}
