% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rest_inference.R
\name{sfr_predict_rest}
\alias{sfr_predict_rest}
\title{Run model inference directly via the SPCS REST endpoint}
\usage{
sfr_predict_rest(
  endpoint,
  new_data,
  token = NULL,
  conn = NULL,
  method = "predict",
  timeout_sec = 30
)
}
\arguments{
\item{endpoint}{An \code{sfr_endpoint} object from \code{\link[=sfr_service_endpoint]{sfr_service_endpoint()}},
or a character URL string.}

\item{new_data}{A data.frame with input data.}

\item{token}{Character or NULL. Auth token (PAT or JWT). If NULL, resolved
via \code{\link[=sfr_pat]{sfr_pat()}} (reads \code{SNOWFLAKE_PAT} env var, or auto-generates a JWT
from \code{conn}).}

\item{conn}{An \code{sfr_connection} object, or NULL. Used for auto-generating
a JWT when no explicit token or PAT env var is available.}

\item{method}{Character. Model method name. Default: \code{"predict"}.
Underscores are converted to dashes in the URL per Snowflake convention.}

\item{timeout_sec}{Numeric. Request timeout in seconds. Default: 30.}
}
\value{
A data.frame with predictions.
}
\description{
Sends a data.frame to the model's HTTP endpoint and returns predictions.
This is the fastest inference path -- pure R, no Python bridge.
}
\examples{
\dontrun{
ep <- sfr_service_endpoint(conn, "mpg_service")
preds <- sfr_predict_rest(ep, new_data, conn = conn)
}

}
