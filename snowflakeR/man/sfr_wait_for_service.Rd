% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{sfr_wait_for_service}
\alias{sfr_wait_for_service}
\title{Wait for an SPCS service to be ready}
\usage{
sfr_wait_for_service(
  reg,
  service_name,
  timeout_min = 10,
  poll_sec = 15,
  verbose = TRUE
)
}
\arguments{
\item{reg}{An \code{sfr_model_registry} or \code{sfr_connection} object. Used to
resolve the database and schema where the service lives.}

\item{service_name}{Character. Name of the SPCS service.}

\item{timeout_min}{Numeric. Maximum minutes to wait. Default: 10.}

\item{poll_sec}{Numeric. Seconds between status checks. Default: 15.}

\item{verbose}{Logical. Print status updates? Default: \code{TRUE}.}
}
\value{
\code{TRUE} (invisibly) if the service is running; raises an error on
terminal failure or timeout.
}
\description{
Polls \code{\link[=sfr_get_service_status]{sfr_get_service_status()}} until the service reaches \code{READY}/\code{RUNNING}
or a terminal failure state is detected. Useful after \code{\link[=sfr_deploy_model]{sfr_deploy_model()}}
since SPCS services take several minutes to provision.
}
\examples{
\dontrun{
sfr_deploy_model(reg, "MY_MODEL", "V1", "my_svc", "ML_POOL", "my_repo")
sfr_wait_for_service(reg, "my_svc", timeout_min = 15)
}

}
