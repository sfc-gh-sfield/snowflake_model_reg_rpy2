% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{sfr_fqn}
\alias{sfr_fqn}
\title{Build a fully qualified table name}
\usage{
sfr_fqn(conn, table_name, database = NULL, schema = NULL)
}
\arguments{
\item{conn}{An \code{sfr_connection} object.}

\item{table_name}{Character. Unqualified table name.}

\item{database}{Character. Database name override. Default: connection's
current database.}

\item{schema}{Character. Schema name override. Default: connection's
current schema.}
}
\value{
Character. Fully qualified table name.
}
\description{
Constructs \code{DATABASE.SCHEMA.TABLE_NAME} from the connection context.
Ensures all notebook table references are fully qualified as recommended
by Snowflake for Workspace Notebooks.
}
\details{
By default, uses the connection's current database and schema. Pass
\code{database} and/or \code{schema} to override -- useful when referencing tables
in a different schema or database without changing the session context.
}
\examples{
\dontrun{
# Uses connection's current database/schema
sfr_fqn(conn, "MY_TABLE")
# => "MY_DB.MY_SCHEMA.MY_TABLE"

# Override schema (same database)
sfr_fqn(conn, "MY_TABLE", schema = "OTHER_SCHEMA")
# => "MY_DB.OTHER_SCHEMA.MY_TABLE"

# Override both
sfr_fqn(conn, "MY_TABLE", database = "OTHER_DB", schema = "RAW")
# => "OTHER_DB.RAW.MY_TABLE"
}

}
